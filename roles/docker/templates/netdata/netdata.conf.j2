# Managed by Ansible. Ensures Netdata persists metrics using dbengine.
[global]
  memory mode = dbengine
  dbengine disk space MB = {{ netdata_dbengine_disk_space_mb | default(2048) }}
  page cache size = {{ netdata_page_cache_size_mb | default(64) }}
  rebuild databases = yes
