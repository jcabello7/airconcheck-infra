/* Subtle modern extras */
.group-title { text-transform: uppercase; letter-spacing: 0.06em; }
.service-card .description { opacity: 0.8; }

/* Extra spacing between curated links and containers */
/* Homepage generates ids from group names: lowercase and hyphenated */
#enlaces-produccion { margin-bottom: 2rem; }
